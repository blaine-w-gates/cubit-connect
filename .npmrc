# Enable legacy peer dependency resolution
# This allows eslint@8.x to work with next@16.x and eslint-config-next@14.x
# without strict peer dependency conflicts
legacy-peer-deps=true

# Also disable strict peer deps for additional flexibility
strict-peer-deps=false
